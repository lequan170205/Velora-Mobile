import type { AllowedVideoType } from '../types/reel.types'

export const DEFAULT_REELS_LIMIT = 10

export const allowedVideoTypes: AllowedVideoType[] = ['video/mp4', 'video/webm', 'video/quicktime']
